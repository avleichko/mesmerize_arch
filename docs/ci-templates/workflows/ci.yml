# REFERENCE ONLY — not executed in the mesmerize docs repo.
# Copy to a platform monorepo as: .github/workflows/ci.yml
# Stack: Python / FastAPI Platform API (ADR-017) + optional Node frontends (polyglot).
# Source patterns: touchscreen-ux .github/workflows/ci.yml (Confirmed there; Proposed here).
# Spec: docs/superpowers/specs/2026-07-23-ci-templates-adoption-from-touchscreen-ux-design.md
#
# Do NOT add: Netlify, TTV, whitelabel, content-links, contrast/axe, eringest, placeholder-scan.

name: CI

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]

jobs:
  # --- Ladder A: Platform API (Python) ---
  api-lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          # TODO: pin per Q-15 (recommend 3.12+)
          python-version: '3.12'
      - name: Install (uv or poetry)
        run: |
          # TODO: e.g. pip install uv && uv sync --frozen
          #   or: pip install poetry && poetry install --no-root
          echo "TODO: install Python dependencies"
          exit 1
      - name: Ruff
        run: |
          # TODO: uv run ruff check .   OR   poetry run ruff check .
          echo "TODO: ruff"
          exit 1

  api-typecheck:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install
        run: |
          echo "TODO: install Python dependencies"
          exit 1
      - name: Mypy
        run: |
          # TODO: uv run mypy src
          echo "TODO: mypy"
          exit 1

  api-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install
        run: |
          echo "TODO: install Python dependencies"
          exit 1
      - name: Pytest
        run: |
          # TODO: uv run pytest -q
          echo "TODO: pytest"
          exit 1

  api-image:
    needs: [api-typecheck, api-test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build API container
        run: |
          # TODO: docker build -t mesmerize-api:$GITHUB_SHA apps/api
          # Later: push ECR — deploy strategy Unknown (ADR-016)
          echo "TODO: docker build (Python base image)"
          exit 1

  # --- Optional: TypeScript frontends in the same polyglot repo ---
  web-ci:
    runs-on: ubuntu-latest
    # Remove this job if frontends live in a separate repo / Ladder B only.
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          # cache: 'pnpm'
      - name: Install
        run: |
          # TODO: corepack enable && pnpm install --frozen-lockfile
          echo "TODO: pnpm install (frontends)"
          exit 1
      - name: Lint / typecheck / test / build
        run: |
          # TODO: pnpm lint && pnpm typecheck && pnpm test && pnpm build
          # Scope to smart-launcher / exam-room / waiting-room as applicable
          echo "TODO: frontend CI"
          exit 1
